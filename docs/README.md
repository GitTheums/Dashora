# Dashora documentation

Architecture and contributor notes will live here as the project grows.

## Current status

Scaffolded monorepo with:

- `apps/web` — minimal development landing page
- `apps/server` — `GET /api/v1/health`
- Shared packages for UI, schemas, and widget contracts

No business features yet.
